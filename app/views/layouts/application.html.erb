<!DOCTYPE html>
<html>
  <head>
  <link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Lobster" />
    <title>Rorgram</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <div class="container">
    <a class="navbar-brand" href="#"><%= image_tag("logo-instagram.png", :alt => "logo", :width => "40") %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item project-name">
          RORGram
        </li>
      </ul>
      <div class="search">
        <form class="form-inline my-2 my-lg-0 nana">
          <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        </form>
      </div>
      <% if current_user %>
      <div class="user-btn">
          <%= link_to "<i class='fa fa-user'></i>".html_safe, controller: "profile",action: "profile" %>
          <%= current_user.username %>
          <%= link_to "Log out", session_path('destroy'), method: :delete %>
      </div>
      <% else %>
        <div class="user-btn">
          <%= link_to "Sing up", new_user_path %>
        </div>
        <div class="user-btn">
          <%= link_to "Log in", new_session_path %>
        </div>
      <% end %>
    </div>
    </div>
  </nav>
    <%= yield %>
  </body>
</html>
